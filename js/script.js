function funcmodelo(){
marca = window.document.f1.marca.value;
modelo = window.document.f1.modelo;

modelo.innerHTML = "";

if (marca == "audi"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A1 Sportback');
    optionElement.value = 'a1sportback';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A3 Sedan');
    optionElement.value = 'a3sedan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A3 Sportback');
    optionElement.value = 'a3sportback';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A4 Avant');
    optionElement.value = 'a4avant';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A4 Limited Edition');
    optionElement.value = 'a4limitededition';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A4 Sedan');
    optionElement.value = 'a4sedan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A5 Sportback');
    optionElement.value = 'a5sportback';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A6 Sedan');
    optionElement.value = 'a6sedan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A7 Sportback');
    optionElement.value = 'a7sportback';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('A8');
    optionElement.value = 'a8';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Q3');
    optionElement.value = 'q3';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Q3 Black Edition');
    optionElement.value = 'q3blackedition';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Q5');
    optionElement.value = 'q5';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Q7');
    optionElement.value = 'q7';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('R8 Coupé Plus');
    optionElement.value = 'r8coupeplus';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('RS 6 Avant Performance');
    optionElement.value = 'rs6avantperformance';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('RS 7 Sportback Performance');
    optionElement.value = 'rs7sportbackperformance';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('RS Q3');
    optionElement.value = 'rsq3';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('RS3');
    optionElement.value = 'rs3';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('S8 Plus');
    optionElement.value = 's8plus';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('SQ5');
    optionElement.value = 'sq5';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('TT Coupé');
    optionElement.value = 'ttcoupe';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('TT Roadster');
    optionElement.value = 'ttroadster';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('TT RS');
    optionElement.value = 'ttrs';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('TTS Coupé');
    optionElement.value = 'ttcoupe';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('TTS Roadster');
    optionElement.value = 'ttroadster';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

}

if (marca == "bentley"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bentayga Diesel');
    optionElement.value = 'bentaygadiesel';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bentayga Hybrid');
    optionElement.value = 'bentaygahybrid';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bentayga Mulliner');
    optionElement.value = 'bentaygamulliner';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bentayga Speed');
    optionElement.value = 'bentaygaspeed';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bentayga V8');
    optionElement.value = 'bentaygav8';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bentayga V8 Design Series');
    optionElement.value = 'bentaygav8designseries';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Mulsanne');
    optionElement.value = 'mulsanne';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Mulsanne Extended Wheelbase');
    optionElement.value = 'mulsanneextendedwheelbase';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Mulsanne Speed');
    optionElement.value = 'mulsannespeed';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Continental');
    optionElement.value = 'newcontinental';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Continental GT');
    optionElement.value = 'newcontinentalgt';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Continental GT Convertible');
    optionElement.value = 'newcontinentalgtconvertible';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Continental GT V8');
    optionElement.value = 'newcontinentalgtv8';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Continental GT V8 Convertible');
    optionElement.value = 'newcontinentalgtv8convertible';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Flying Spur');
    optionElement.value = 'newflyingspur';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "bmw"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('530e Hybrid');
    optionElement.value = '530ehybrid';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('i3');
    optionElement.value = 'i3';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('M2 Competition');
    optionElement.value = 'm2competition';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('M5');
    optionElement.value = 'm5';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Série 1');
    optionElement.value = 'serie1';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Série 2 Gran Coupé');
    optionElement.value = 'serie2grancoupe';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Série 3 Sedan');
    optionElement.value = 'serie3sedan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Série 4 Cabrio');
    optionElement.value = 'serie4cabrio';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Série 7 Hybrid');
    optionElement.value = 'serie7hybrid';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Série 8 Coupé');
    optionElement.value = 'serie8coupe';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X1');
    optionElement.value = 'x1';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X2');
    optionElement.value = 'x2';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X2 M35i');
    optionElement.value = 'x2m35i';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X3');
    optionElement.value = 'x3';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X3 M');
    optionElement.value = 'x3m';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X4');
    optionElement.value = 'x4';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X4 M Competition');
    optionElement.value = 'x4mcompetition';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X5');
    optionElement.value = 'x5';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X6');
    optionElement.value = 'x6';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('X7');
    optionElement.value = 'x7';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Z4');
    optionElement.value = 'z4';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Z4 M4Oi');
    optionElement.value = 'z4m4oi';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

}

if (marca == "caoaChery"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Arrizo 5');
    optionElement.value = 'arrizo5';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Celer');
    optionElement.value = 'celer';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Novo Tiggo 5X 2021');
    optionElement.value = 'novotiggo5x2021';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Novo Tiggo 7 2021');
    optionElement.value = 'novotiggo72021';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Tiggo 2');
    optionElement.value = 'tiggo2';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Tiggo 5X 2020');
    optionElement.value = 'tiggo5x2020';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Tiggo 7 2020');
    optionElement.value = 'tiggo72020';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "chevrolet"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Agile');
    optionElement.value = 'agile';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Avalanche');
    optionElement.value = 'avalanche';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Aveo');
    optionElement.value = 'Aveo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Blazer');
    optionElement.value = 'blazer';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bolt');
    optionElement.value = 'bolt';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Camaro');
    optionElement.value = 'camaro';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Captiva');
    optionElement.value = 'captiva';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Celta');
    optionElement.value = 'celta';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classic');
    optionElement.value = 'classic';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Cobalt');
    optionElement.value = 'cobalt';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Colorado');
    optionElement.value = 'colorado';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Corsa');
    optionElement.value = 'corsa';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Corvette');
    optionElement.value = 'corvette';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Cruze');
    optionElement.value = 'cruze';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Equinox');
    optionElement.value = 'equinox';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Express');
    optionElement.value = 'express';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HHR');
    optionElement.value = 'hhr';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Impala');
    optionElement.value = 'impala';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Joy');
    optionElement.value = 'joy';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Kodiak');
    optionElement.value = 'kodiak';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Malibu');
    optionElement.value = 'malibu';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Meriva');
    optionElement.value = 'meriva';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Montana');
    optionElement.value = 'montana';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Monza');
    optionElement.value = 'monza';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Niva');
    optionElement.value = 'niva';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Omega');
    optionElement.value = 'omega';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Onix');
    optionElement.value = 'onix';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Opala');
    optionElement.value = 'opala';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Prisma');
    optionElement.value = 'prisma';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Silverado');
    optionElement.value = 'silverado';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Spark');
    optionElement.value = 'spark';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Spin');
    optionElement.value = 'spin';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Suburban');
    optionElement.value = 'suburban';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('S10');
    optionElement.value = 's10';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('SS');
    optionElement.value = 'ss';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Tahoe');
    optionElement.value = 'tahoe';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('TrailBlazer');
    optionElement.value = 'trailblazer';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Traverse');
    optionElement.value = 'traverse';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Trax');
    optionElement.value = 'trax';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Uplander');
    optionElement.value = 'uplander';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Vectra');
    optionElement.value = 'vectra';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Volt');
    optionElement.value = 'volt';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Zafira');
    optionElement.value = 'zafira';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

}

if (marca == "citroen"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('2CV');
    optionElement.value = '2cv';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Aircross');
    optionElement.value = 'aircross';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ami');
    optionElement.value = 'ami';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Berlingo');
    optionElement.value = 'berlingo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('BX');
    optionElement.value = 'bx';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('C1');
    optionElement.value = 'c1';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('C3');
    optionElement.value = 'c3';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('C3 Aircross');
    optionElement.value = 'c3aircross';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('C4');
    optionElement.value = 'c4';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('C4 Cactus');
    optionElement.value = 'c4cactus';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('C4 Lounge');
    optionElement.value = 'c4lounge';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('C4 Space Tourer');
    optionElement.value = 'c4spacetourer';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('C5');
    optionElement.value = 'c5';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Dyane');
    optionElement.value = 'dyane';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('DS');
    optionElement.value = 'ds';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('DS5');
    optionElement.value = 'ds5';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('GS');
    optionElement.value = 'gs';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('H Van');
    optionElement.value = 'hvan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Jumper');
    optionElement.value = 'jumper';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
    
    optionElement = document.createElement('option');
    texto = document.createTextNode('Jumpy');
    optionElement.value = 'jumpy';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Traction Avant');
    optionElement.value = 'tractionavant';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Visa');
    optionElement.value = 'visa';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('XM');
    optionElement.value = 'xm';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
    
}

if (marca == "dodge"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement("option");
    texto = document.createTextNode('Aries');
    optionElement.value = 'aries';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Avenger');
    optionElement.value = 'avenger';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Caliber');
    optionElement.value = 'caliber';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Caravan');
    optionElement.value = 'caravan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Challenger');
    optionElement.value = 'challenger';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Charger');
    optionElement.value = 'charger';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Dakota');
    optionElement.value = 'dakota';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Dart');
    optionElement.value = 'dart';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Demon Concept');
    optionElement.value = 'demonconcept';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Durango');
    optionElement.value = 'durango';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Dynasty');
    optionElement.value = 'dynasty';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fluid Drive');
    optionElement.value = 'fluiddrive';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Interpid');
    optionElement.value = 'interpid';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Journey');
    optionElement.value = 'journey';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('LeBaron');
    optionElement.value = 'lebaron';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Magnum');
    optionElement.value = 'magnum';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Nitro');
    optionElement.value = 'nitro';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Omni 024');
    optionElement.value = 'omni024';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Polara');
    optionElement.value = 'polara';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ram');
    optionElement.value = 'ram';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ram 50');
    optionElement.value = 'ram50';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ram SRT 10');
    optionElement.value = 'ramsrt10';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Spirit');
    optionElement.value = 'spirit';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Stratus');
    optionElement.value = 'stratus';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Super Bee');
    optionElement.value = 'superbee';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Tomahawk');
    optionElement.value = 'tomahawk';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Town Wagon');
    optionElement.value = 'townwagon';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Venom');
    optionElement.value = 'venom';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Viper');
    optionElement.value = 'viper';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Warlock');
    optionElement.value = 'warlock';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('ZEO');
    optionElement.value = 'zeo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "fiat"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Argo');
    optionElement.value = 'argo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Cronos');
    optionElement.value = 'cronos';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Doblò');
    optionElement.value = 'doblo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ducato');
    optionElement.value = 'ducato';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fiorino');
    optionElement.value = 'fiorino';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Grand Siena');
    optionElement.value = 'grandsiena';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Mobi');
    optionElement.value = 'mobi';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Strada');
    optionElement.value = 'strada';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Toro');
    optionElement.value = 'toro';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Uno');
    optionElement.value = 'uno';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Weekend');
    optionElement.value = 'weekend';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);


}

if (marca == "ford"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('EcoSport');
    optionElement.value = 'ecosport';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Edge');
    optionElement.value = 'edge';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Escort');
    optionElement.value = 'escort';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fiesta');
    optionElement.value = 'fiesta';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Focus');
    optionElement.value = 'focus';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Focus Hatch');
    optionElement.value = 'focushatch';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Focus Sedan');
    optionElement.value = 'focussedan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fusion');
    optionElement.value = 'fusion';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ka');
    optionElement.value = 'ka';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ka Sedan');
    optionElement.value = 'kasedan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Mustang');
    optionElement.value = 'mustang';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Fiesta');
    optionElement.value = 'newfiesta';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Fiesta Hatch');
    optionElement.value = 'newfiestahatch';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Fiesta Sedan');
    optionElement.value = 'newfiestasedan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ranger');
    optionElement.value = 'ranger';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Ranger(Cabine Dupla)');
    optionElement.value = 'rangercabinedupla';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Versailles');
    optionElement.value = 'versailles';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "honda"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Accord');
    optionElement.value = 'accord';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('City DX');
    optionElement.value = 'citydx';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('City EX');
    optionElement.value = 'cityex';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('City EXL');
    optionElement.value = 'cityexl';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('City LX');
    optionElement.value = 'citylx';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('City Personal');
    optionElement.value = 'citypersonal';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Civic EX');
    optionElement.value = 'civicex';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Civic EXL');
    optionElement.value = 'civicexl';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Civic LX');
    optionElement.value = 'civiclx';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Civic Si');
    optionElement.value = 'civicsi';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Civic Sport');
    optionElement.value = 'civicsport';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Civic Touring');
    optionElement.value = 'civictouring';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('CR-V');
    optionElement.value = 'crv';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('FIT DX');
    optionElement.value = 'fitdx';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('FIT EX');
    optionElement.value = 'fitex';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('FIT EXL');
    optionElement.value = 'fitexl';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('FIT LX');
    optionElement.value = 'fitlx';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('FIT Personal');
    optionElement.value = 'fitpersonal';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HR-V EX');
    optionElement.value = 'hrvex';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HR-V EXL');
    optionElement.value = 'hrvexl';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HR-V LX');
    optionElement.value = 'hrvlx';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HR-V Touring');
    optionElement.value = 'hrvtouring';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('WR-V EX');
    optionElement.value = 'wrvex';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('WR-V EXL');
    optionElement.value = 'wrvexl';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

}

if (marca == "hyundai"){

    optionElement = document.createElement("option");
    texto = document.createTextNode("");
    optionElement.value = "";
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('CRETA');
    optionElement.value = 'creta';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HB20 Nova Geração');
    optionElement.value = 'hb20novageracao';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HB20 Sport Nova Geração');
    optionElement.value = 'hb20sportnovageracao';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HB20S Nova Geração');
    optionElement.value = 'hb20snovageracao';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('HB20X Nova Geração');
    optionElement.value = 'hb20xnovageracao';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Azera');
    optionElement.value = 'newazera';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New IX35');
    optionElement.value = 'newix35';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Tucson');
    optionElement.value = 'newtucson';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('SANTA FÉ');
    optionElement.value = 'santafe';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

}

if (marca == "jac"){

    optionElement = document.createElement("option");
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('IEV20');
    optionElement.value = 'iev20';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('IEV330P');
    optionElement.value = 'iev330p';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('IEV40');
    optionElement.value = 'iev40';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('IEV60');
    optionElement.value = 'iev60';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('T40 cvt');
    optionElement.value = 't40cvt';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('T40 mt');
    optionElement.value = 't40mt';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('T50');
    optionElement.value = 't50';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('T60');
    optionElement.value = 't60';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('T80');
    optionElement.value = 't80';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "jeep"){

    optionElement = document.createElement("option");
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement("option");
    texto = document.createTextNode('Renegade');
    optionElement.value = 'renegade';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "kia"){

    optionElement = document.createElement("option");
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bongo');
    optionElement.value = 'bongo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Sorento');
    optionElement.value = 'sorento';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Soul');
    optionElement.value = 'soul';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}
if (marca == "landrover"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Defender');
    optionElement.value = 'defender';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Discovery');
    optionElement.value = 'discovery';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Freelander');
    optionElement.value = 'freelander';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('LRX');
    optionElement.value = 'lrx';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Range Rover');
    optionElement.value = 'rangerover';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Range Rover Sport');
    optionElement.value = 'rangeroversport';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Wolf');
    optionElement.value = 'wolf';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

}

if (marca == "mercedesbenz"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe A');
    optionElement.value = 'classea';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe B');
    optionElement.value = 'classeb';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe C');
    optionElement.value = 'classec';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe CL');
    optionElement.value = 'classecl';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe CLC');
    optionElement.value = 'classeclc';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe CLK');
    optionElement.value = 'classeclk';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe CLS');
    optionElement.value = 'classecls';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe E');
    optionElement.value = 'classee';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe G');
    optionElement.value = 'classeg';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe GL');
    optionElement.value = 'classegl';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe GLA');
    optionElement.value = 'classegla';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe GLC');
    optionElement.value = 'classeglc';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe GLK');
    optionElement.value = 'classeglk';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe M');
    optionElement.value = 'classem';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe R');
    optionElement.value = 'classer';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe S');
    optionElement.value = 'classes';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe SL');
    optionElement.value = 'classesl';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe SLK');
    optionElement.value = 'classeslk';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe V');
    optionElement.value = 'classev';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Classe X');
    optionElement.value = 'classex';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('CLR');
    optionElement.value = 'clr';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Sprinter');
    optionElement.value = 'sprinter';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "mitsubishi"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Carisma');
    optionElement.value = 'carisma';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Galant');
    optionElement.value = 'galant';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Lancer');
    optionElement.value = 'lancer';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Lancer Evolution');
    optionElement.value = 'lancerevolution';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Mirage');
    optionElement.value = 'mirage';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Outlander');
    optionElement.value = 'outlander';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Pajero');
    optionElement.value = 'pajero';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Pajero Pinin');
    optionElement.value = 'pajeropinin';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Pajero Sport');
    optionElement.value = 'pajerosport';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "nissan"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Armada');
    optionElement.value = 'armada';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Frontier');
    optionElement.value = 'frontier';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Grand Livina');
    optionElement.value = 'grandlivina';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Jonga');
    optionElement.value = 'jonga';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Kicks');
    optionElement.value = 'kicks';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Largo');
    optionElement.value = 'largo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Livina');
    optionElement.value = 'livina';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Maxima');
    optionElement.value = 'maxima';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Micra');
    optionElement.value = 'micra';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Pathfinder');
    optionElement.value = 'pathfinder';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Patrol');
    optionElement.value = 'patrol';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Sunny');
    optionElement.value = 'sunny';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Versa');
    optionElement.value = 'versa';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "peugeot"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('106');
    optionElement.value = '106';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('208');
    optionElement.value = '208';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('206');
    optionElement.value = '206';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('206 Escapade');
    optionElement.value = '206escapade';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('207');
    optionElement.value = '207';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('208');
    optionElement.value = '208';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('304');
    optionElement.value = '304';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('306');
    optionElement.value = '306';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('307');
    optionElement.value = '307';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('308');
    optionElement.value = '308';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('309');
    optionElement.value = '309';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('405');
    optionElement.value = '405';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('406');
    optionElement.value = '406';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('407');
    optionElement.value = '407';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('408');
    optionElement.value = '408';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('508');
    optionElement.value = '508';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('605');
    optionElement.value = '605';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('607');
    optionElement.value = '607';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Hoggar');
    optionElement.value = 'hoggar';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "renault"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Clio');
    optionElement.value = 'clio';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Duster');
    optionElement.value = 'duster';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Duster Oroch');
    optionElement.value = 'dusteroroch';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fluence');
    optionElement.value = 'fluence';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Kwid');
    optionElement.value = 'kwid';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Logan');
    optionElement.value = 'logan';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Sandero');
    optionElement.value = 'sandero';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Stand Up');
    optionElement.value = 'standup';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "suzuki"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Jimny');
    optionElement.value = 'jimny';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "toyota"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Aurion');
    optionElement.value = 'aurion';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bandeirante');
    optionElement.value = 'bandeirante';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Belta');
    optionElement.value = 'belta';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Land Cruiser');
    optionElement.value = 'landcruiser';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Etios');
    optionElement.value = 'etios';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fortuner');
    optionElement.value = 'fortuner';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Hilux');
    optionElement.value = 'hilux';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Passo');
    optionElement.value = 'passo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Verso');
    optionElement.value = 'verso';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);
}

if (marca == "volkswagen"){

    optionElement = document.createElement('option');
    texto = document.createTextNode('');
    optionElement.value = '';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Amarok');
    optionElement.value = 'amarok';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Apollo');
    optionElement.value = 'apollo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Atlas');
    optionElement.value = 'atlas';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Bora');
    optionElement.value = 'bora';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Brasília');
    optionElement.value = 'brasilia';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Caddy');
    optionElement.value = 'caddy';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Citi Golf');
    optionElement.value = 'citigolf';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Corrado');
    optionElement.value = 'corrado';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Crafter');
    optionElement.value = 'crafter';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('CrossGolf');
    optionElement.value = 'crossgolf';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('E-Up!');
    optionElement.value = 'eup';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fox');
    optionElement.value = 'fox';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fusca');
    optionElement.value = 'fusca';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Fusca A5');
    optionElement.value = 'fuscaa5';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Gol');
    optionElement.value = 'gol';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Golf');
    optionElement.value = 'golf';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Kombi');
    optionElement.value = 'kombi';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Lupo');
    optionElement.value = 'lupo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('New Beetle');
    optionElement.value = 'newbeetle';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Parati');
    optionElement.value = 'parati';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Polo');
    optionElement.value = 'polo';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Polo Classic');
    optionElement.value = 'poloclassic';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Santana');
    optionElement.value = 'santana';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Saveiro');
    optionElement.value = 'saveiro';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('SpaceFox');
    optionElement.value = 'spacefox';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Touran');
    optionElement.value = 'touran';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Up!');
    optionElement.value = 'up';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Virtus');
    optionElement.value = 'virtus';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

    optionElement = document.createElement('option');
    texto = document.createTextNode('Voyage');
    optionElement.value = 'voyage';
    modelo.appendChild(optionElement);
    optionElement.appendChild(texto);

}

}   